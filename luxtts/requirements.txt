# LuxTTS microservice dependencies
# Install with: pip install -r requirements.txt

# LuxTTS model — install from the repo
# git clone https://github.com/ysharma3501/LuxTTS.git && pip install -e ./LuxTTS

# FastAPI server
fastapi>=0.111.0
uvicorn[standard]>=0.30.0

# Audio I/O
soundfile>=0.12.1
numpy>=1.24.0

# LuxTTS core deps (also pulled in by LuxTTS install, listed here for clarity)
torch>=2.1.0
torchaudio>=2.1.0
huggingface_hub>=0.24.0
transformers<=4.57.6
vocos
pydub
librosa
